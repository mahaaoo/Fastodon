import 'package:flutter/material.dart';

class MyColor {
  static Color loginPrimary = Color.fromRGBO(115, 167, 238, 1);
  static Color loginWhite = Colors.white;
  static Color loginBackgroundColor = Color.fromRGBO(240, 240, 240, 1);
  static Color aLiveColor = Color.fromRGBO(121, 189, 154, 1);

  static Color error = Colors.red;
  static Color greyText = Colors.grey[500];
  static Color homeIconColor = Colors.grey[500];
  static Color dividerLineColor = Color.fromRGBO(240, 240, 240, 1);
}
